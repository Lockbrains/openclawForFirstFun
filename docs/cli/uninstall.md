---
summary: "CLI reference for `firstclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `firstclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
firstclaw uninstall
firstclaw uninstall --all --yes
firstclaw uninstall --dry-run
```
